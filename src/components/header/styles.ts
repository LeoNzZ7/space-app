import styled from "styled-components";

export const Header = styled.header`
    padding: 60px 20px;
    display: flex;
    justify-content: space-between;

    img {
        max-width: 212px;
    }
`;